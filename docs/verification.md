# 本次检查结果

## 自动检查

- `pnpm check`：ESLint 零错误、零警告；TypeScript 检查通过。
- `pnpm build`：Next.js 静态导出成功，四个主页面和自定义 404 生成完整。
- `pnpm verify:export`：五个 HTML 文件与 29 个独立本地 URL 检查通过，包含图片 src/srcSet、JS、CSS、favicon、页面链接和锚点；全部位于 `/car/` 下。
- 使用自带静态预览服务逐个请求：29 个 URL 的 GET/HEAD 正常，JS/CSS 内容类型正确，`/car` 与不带末尾斜杠的故事路径正确重定向；缺失页面返回自定义 404，POST 返回 405。
- 对重构前后的静态正文去除空白后比较：故事页、体验页、404 文案一致；首页仅删除原来始终隐藏的装饰句；悬赏页只出现统一支付政策后的标点与“奖励”字样差异。
- 应用页面与业务组件不再包含直接写死的中文文案；QQ 数字只在 `content/site.ts` 定义。
- 清理后的锁文件通过 pnpm 的冻结锁文件一致性与现有供应链策略检查。依赖版本及完整性值未升级；生产构建复用已安装依赖，本次未在全新机器重新联网安装全部依赖。

## 浏览器检查

使用 Chromium 中的手机宽度与桌面宽度容器检查；这是响应式布局检查，不是真实 iPhone 模拟。

- 首页两张照片加载正常，手机容器没有横向溢出。
- 照片弹窗可打开、切换另一侧并关闭，关闭后焦点返回原入口。
- 导航可以进入故事、悬赏与体验页面。
- 角色标签点击与键盘方向键切换正常，当前标签和面板对应。
- 联系弹窗展示正确奖励与 QQ，号码及投稿提纲可复制；号码剪贴板结果为 `1765875868`。
- 联系弹窗关闭后焦点返回原入口；FAQ 可展开，体验页联系入口正常。
- 桌面容器也未出现横向溢出。

## 未覆盖的环境

未连接真实 iPhone，也未操作你的服务器。Safari/QQ/微信内置浏览器的触控、系统边缘返回、地址栏伸缩、用户权限差异仍需上线后真机检查。服务器的 Nginx 规则、CDN 缓存及实际上传结果不属于本地验证结果。

`pnpm build` 已包含资源检查；日后每次改文案或图片都可以重复执行。不要把本地验证理解为全部浏览器和所有未来修改都已验证。
