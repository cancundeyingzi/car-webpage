# 合并到 i.ccdyz.top 现有的 server 块，不要替换整个配置。
# 继承的 root 指向域名网站根目录；该目录下存在 car/index.html。
# 已有同名 location 时修改原规则，不要重复添加。
location = /car {
    return 301 /car/;
}
location ^~ /car/ {
    index index.html;
    error_page 404 /car/404.html;
    try_files $uri $uri/ =404;
}
